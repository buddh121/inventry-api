'use strict';

module.exports = function(role) {

/**
 * Fetch Role of the user
 * @param {string} userId user id of logged in user
 * @param {string} userId
 * @param {Function(Error)} callback
 */

role.userRole = function(userId, callback) {

	console.log(userId)
  // TODO
  callback(null);
};

};
